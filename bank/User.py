#!/usr/bin/env python3

class User:
	def __init__(self, first_name, last_name, pin_number, username, password):
		self.first_name = first_name
		self.last_name = last_name
		self.pin_number = pin_number
		self.username = username
		self.password = password


